@model VidlyMovieRental.Models.Customer
@{
    ViewBag.Title = "Details";
}

<h2>@Model.Name</h2>

<ul>
    <li>Membership Type: @Model.MembershipType.Name</li>
    @if (Model.BirthDate.HasValue)
    {
        <li>Date Of Birth: @Model.BirthDate.Value.ToShortDateString()</li>
    }

</ul>

